{
  "name": "@sentenza/extension",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --mode content-hook && vite build --mode content-bridge",
    "lint": "eslint .",
    "check-types": "tsc --noEmit --project tsconfig.check.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@sentenza/domain": "workspace:*"
  },
  "devDependencies": {
    "@sentenza/eslint-config": "workspace:*",
    "@sentenza/typescript-config": "workspace:*",
    "@sentenza/vitest-config": "workspace:*",
    "@types/chrome": "0.3.0",
    "@types/node": "22.20.4",
    "typescript": "5.7.3",
    "vite": "7.3.6",
    "vitest": "3.2.7",
    "zod": "3.25.76"
  }
}
