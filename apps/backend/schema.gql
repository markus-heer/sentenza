# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""Das erneuerte Access-Token mit seinem Ablaufzeitpunkt."""
type AccessTokenPayload {
  """Das neu ausgestellte Sentenza_Access_Token."""
  accessToken: String!

  """Zeitpunkt, zu dem das Access-Token seine Gültigkeit verliert."""
  accessTokenExpiresAt: DateTime!
}

"""
Access- und Refresh-Token einer erfolgreichen Anmeldung, jeweils mit Ablaufzeitpunkt.
"""
type AuthPayload {
  """Das Sentenza_Access_Token für geschützte Operationen."""
  accessToken: String!

  """Zeitpunkt, zu dem das Access-Token seine Gültigkeit verliert."""
  accessTokenExpiresAt: DateTime!

  """
  Das Sentenza_Refresh_Token für die Erneuerung ohne neue Google-Anmeldung.
  """
  refreshToken: String!

  """Zeitpunkt, zu dem das Refresh-Token seine Gültigkeit verliert."""
  refreshTokenExpiresAt: DateTime!
}

"""Das Niveau eines Grammatik_Thema nach dem europäischen Referenzrahmen."""
enum CefrLevel {
  A1
  A2
  B1
  B2
  C1
  UNBEKANNT
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

"""
Platzhalter-Typ, hält die Domänen-Enumerationen im generierten Schema sichtbar.
"""
type DomainEnumProbe {
  cefrLevel: CefrLevel!
  payloadKind: PayloadKind!
  processingState: ProcessingState!
  submissionSource: SubmissionSource!
  targetLanguage: TargetLanguage!
}

"""Eine Grammatik_Kategorie des Busuu-Katalogs mit ihren Themen."""
type GrammarCategory {
  """Die Busuu-Kennung der Grammatik_Kategorie, unverändert."""
  busuuId: ID!

  """Die Beschreibung in der Sprache `de`."""
  descriptionDe: String!

  """Die Beschreibung in der Sprache `en`."""
  descriptionEn: String!

  """Die Zielsprache, zu der die Kategorie gehört."""
  language: TargetLanguage!

  """Die Bezeichnung in der Sprache `de`."""
  nameDe: String!

  """Die Bezeichnung in der Sprache `en`."""
  nameEn: String!

  """
  Ob die Grammatik_Kategorie im letzten Katalog-Payload nicht mehr enthalten war.
  """
  removedFromCatalog: Boolean!

  """
  Die zugeordneten Grammatik_Themen, aufsteigend nach Sortierposition und bei Gleichheit nach Busuu-Kennung.
  """
  topics: [GrammarTopic!]!
}

"""
Der Lernstand des angemeldeten Benutzerkontos zu einem Grammatik_Thema.
"""
type GrammarProgress {
  """Zeitpunkt der Einreichung, aus der dieser Lernstand stammt."""
  observedAt: DateTime!

  """Der von Busuu gemeldete Prozentwert, unverändert übernommen."""
  percentage: Int!

  """Die von Busuu gemeldete Stärke, unverändert übernommen."""
  strength: Int!
}

"""Ein Grammatik_Thema des Busuu-Katalogs samt Lernstand."""
type GrammarTopic {
  """Die von Busuu gemeldete Zugangsstufe, unverändert."""
  accessTier: String!

  """Die Busuu-Kennung des Grammatik_Thema, unverändert."""
  busuuId: ID!

  """
  Das Niveau des Grammatik_Thema; `UNBEKANNT`, wenn es nicht abbildbar war.
  """
  cefrLevel: CefrLevel!

  """Die ungekürzte Beschreibung in der Sprache `de`."""
  descriptionDe: String!

  """Die ungekürzte Beschreibung in der Sprache `en`."""
  descriptionEn: String!

  """
  Ob das Grammatik_Thema nur aus einer Struktur bekannt ist und daher ohne Bezeichnung und Beschreibung vorliegt.
  """
  incomplete: Boolean!

  """Die Bezeichnung in der Sprache `de`."""
  nameDe: String!

  """Die Bezeichnung in der Sprache `en`."""
  nameEn: String!

  """Ob Busuu das Grammatik_Thema als zahlungspflichtig führt."""
  premium: Boolean!

  """
  Der Lernstand des angemeldeten Benutzerkontos; `null` bei einem Ungeübten_Thema.
  """
  progress: GrammarProgress

  """
  Ob das Grammatik_Thema im letzten Katalog-Payload nicht mehr enthalten war.
  """
  removedFromCatalog: Boolean!

  """
  Die Position innerhalb der Grammatik_Kategorie; `null`, wenn das Thema in keiner Struktur referenziert ist.
  """
  sortPosition: Int

  """
  Ungeübtes_Thema: Zum angemeldeten Benutzerkonto liegt kein Lernstand zu diesem Grammatik_Thema vor.
  """
  untrained: Boolean!
}

type Mutation {
  """
  Stellt ohne erneute Google-Anmeldung ein neues Access-Token aus; das Refresh-Token bleibt unverändert gültig.
  """
  refreshAccessToken(input: RefreshAccessTokenInput!): AccessTokenPayload!

  """
  Meldet ein Google-Konto an und gibt Access- und Refresh-Token samt Ablaufzeitpunkten zurück.
  """
  signInWithGoogle(input: SignInWithGoogleInput!): AuthPayload!
}

"""Die Kennzeichnung der Struktur eines eingereichten Payloads."""
enum PayloadKind {
  CATALOG
  PROGRESS
}

"""Der Stand der Normalisierung eines Eintrags im Raw_Payload_Store."""
enum ProcessingState {
  FEHLGESCHLAGEN
  TEILWEISE_VERARBEITET
  VERARBEITET
}

type Query {
  """
  Platzhalter-Query, hält die Domänen-Enumerationen im generierten Schema sichtbar.
  """
  domainEnumProbe: DomainEnumProbe!

  """
  Die Grammatik_Kategorien einer Zielsprache mit ihren Grammatik_Themen und dem Lernstand des angemeldeten Benutzerkontos.
  """
  grammarCatalog(
    """Die Zielsprache, deren Katalog abgefragt wird."""
    language: TargetLanguage!
  ): [GrammarCategory!]!

  """Platzhalter-Query, bis reale Resolver existieren."""
  ping: String!
}

"""Das vorgelegte Refresh-Token einer Erneuerung."""
input RefreshAccessTokenInput {
  """Das bei der Anmeldung erhaltene Sentenza_Refresh_Token."""
  refreshToken: String!
}

"""Das von Google ausgestellte ID-Token einer Anmeldung."""
input SignInWithGoogleInput {
  """Das Google_ID_Token, wie Google es ausgestellt hat."""
  idToken: String!
}

"""Die Angabe, welcher Client einen Payload eingereicht hat."""
enum SubmissionSource {
  SENTENZA_EXTENSION
}

"""Die von Sentenza unterstützte Lernsprache."""
enum TargetLanguage {
  ES
}